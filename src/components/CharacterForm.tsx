import React, { Component } from 'react';

export default class CharacterForm extends Component {

    render(): JSX.Element {

        return (
            <div>
                <span>Form for new characters created here.</span>
            </div>
        );
    }
}